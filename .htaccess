# Enable error display in PHP
php_value display_errors 1

# Enable rewrite engine
RewriteEngine On

# Comment out or remove redirection to disable it
# RewriteCond %{REQUEST_URI} ^/$
# RewriteRule ^$ https://rio-lawis.com/ [R=301,L]

# Allow index.php to work normally without redirection
