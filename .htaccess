# Enable error display in PHP
php_value display_errors 1

# Enable rewrite engine
RewriteEngine On

# If you want to only redirect the root domain (not index.php)
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^$ https://rio-lawis.com/ [R=301,L]

# Allow index.php to work without redirecting it

